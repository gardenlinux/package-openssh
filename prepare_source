version_orig=10.4p1
debian_version="1%${version_orig}-4"
version=1:${version_orig}-4
version_suffix=gl0~bp2150

git_src -b "debian/$debian_version" "https://salsa.debian.org/ssh-team/openssh.git"

# The patches are already applied on salsa
rm $dir/src/debian/patches/series

apply_patches
